import { StyleSheet, Platform } from "react-native"

export const styleTasks = StyleSheet.create({
    textFiller:{
        flex: 1,
        color: '#fff',
        fontFamily: 'sansat_reg',
        f
    }
})